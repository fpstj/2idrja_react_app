// Here will go the code for one instructor.
// There should be an image and paragraph on the right/left of the image.
// The image and the paragraph should be able to be modified later on, in the Instructors components.

import React from 'react';

// Functional component
const Instructor = () => {
  return (
    <div>
      <h1>This is the component for one Instructor.</h1>
      <p>Image on one side, paragraph on the other</p>
    </div>
  );
};

export default Instructor;
